# NextThing 项目技术说明

> 面向维护者和二次开发者。本文件描述当前项目的技术边界、模块职责和关键业务流；运行环境与模型准备请先阅读 `README.md`。

## 1. 总体设计

NextThing 是 Android 单应用项目，采用分层组织：表现层负责页面和状态，领域层表达业务模型和用例，数据层负责本地、远端和系统能力的实现。

```text
presentation  →  domain  ←  data
      ↓             ↓        ↓
 页面/导航       模型/用例   本地数据、远端服务、系统服务
```

关键原则：

- 页面只消费状态并发出用户事件，业务规则收敛到 ViewModel、UseCase 或服务层。
- 任务、地点、提醒、统计和同步都以同一组领域模型为准，避免不同页面各自维护状态。
- 一级页面为首页、任务、创建、统计、我的；二级页面默认隐藏底部导航。
- 历史设计稿不再是实现依据；以当前 `app/src/main` 和本文件为准。

## 2. 目录与模块

| 目录/模块 | 职责 |
|---|---|
| `app/src/main/java/.../presentation` | Compose 页面、组件、导航、主题和 ViewModel |
| `.../domain/model` | Task、Category、Location、Geofence、NotificationStrategy、Achievement、Sync 等业务模型 |
| `.../domain/usecase` | 任务、位置、地理围栏、同步、用户等业务用例 |
| `.../domain/service` | AI、位置、天气、语音、地理围栏等能力抽象 |
| `.../data/local` | 本地实体、DAO、数据库与迁移 |
| `.../data/remote` | 后端 API、DTO、认证与网络调用 |
| `.../data/service` | 领域服务的具体实现 |
| `.../work` | 逾期检查、重复任务生成、通知、早晚报等后台调度 |
| `.../receiver` | 系统广播与地点围栏事件接收 |

## 3. 页面与导航设计

### 3.1 一级页面

| 路由 | 页面 | 业务职责 |
|---|---|---|
| `today` | 首页 | 今日任务、今日完成、当前位置与日历入口 |
| `tasks` | 任务 | 搜索、状态筛选、周/月浏览、进入详情 |
| `create_task` | 创建 | 生成并保存任务草稿 |
| `stats` | 统计 | 时间范围结果、风险、变化与专项分析入口 |
| `settings` | 我的 | 资料、AI、地点、提醒、主题、同步与导出入口 |

### 3.2 关键二级路由

- `task_detail/{taskId}`：任务阅读/编辑与状态处理。
- `create_location`、`map_picker`：地点新建与地图选点分离。
- `geofence_config`、`geofence_location_detail/{locationId}`：地点库、地点详情和编辑。
- `stats_structure/{timeRange}`、`stats_trend/{timeRange}`、`stats_efficiency/{timeRange}`、`stats_ai`：统计专项分析。
- `theme_settings`、`view_preferences`、`ai_config`、`briefing_settings`、`reminder_strategy`、`sync`、`sync_conflicts`、`export_data`：个人设置子页。

## 4. 核心业务流

### 4.1 任务生命周期

```text
创建草稿 → 保存为待办 → 完成
                  ├→ 延期（记录原因）
                  └→ 取消（记录原因）
```

任务可包含分类、优先级、重复规则、子任务、标签、附件、提醒策略和地点关联。重复任务根据规则生成后续执行项；统计与首页使用同一任务状态口径。

### 4.2 地理围栏

```text
任务编辑开启围栏
  → 选择已有地点 / 新建地点
  → 使用当前位置或地图选点
  → 保存地点
  → 回填并绑定当前任务
  → 到达/离开事件触发任务提醒
```

关键约束：

- 当前定位使用高德能力；不保留 Google 定位回退。
- 地图页只返回经纬度和地址草稿，地点页完成命名和最终保存。
- 任务地理围栏关闭时，详情页不显示该任务的围栏配置。
- 地点可设置全局默认半径或按地点启用自定义离散半径档位。

### 4.3 提醒与后台任务

- 提醒策略配置声音、振动、提前时间和通知方式，任务引用策略。
- 后台调度负责逾期转换、重复任务生成、任务通知和早晚报等周期工作。
- 权限拒绝、网络失败、定位失败必须向页面返回明确可行动状态。

### 4.4 统计

统计范围为今日、本周、本月：

- 结果：完成数、总数、完成率。
- 风险：待办、逾期、重要紧急。
- 变化：完成趋势与同期比较。

专项页继承父级时间范围。数据不足时返回空状态，不生成伪趋势、伪效率结论或无意义图表。

### 4.5 同步、冲突与恢复

```text
本地变更 → 立即同步 → 成功时间/失败状态
                    └→ 冲突 → 冲突处理页
云端恢复 → 检查未上传本地内容 → 用户确认 → 覆盖可恢复数据
```

从云端恢复是危险操作：界面必须解释覆盖范围，并要求显式确认；若本地存在未上传内容，恢复应被阻止或先引导同步。

### 4.6 AI 与语音

- AI 可用于自然语言任务解析、智能搜索、统计摘要、建议、周报和早晚报。
- AI 结果必须以用户确认的任务字段或明确文本展示为边界，不能静默写入任务。
- 语音入口依赖独立模型/运行时资源；资源缺失时，页面显示可理解的不可用状态。

## 5. 数据实体关系

```text
User
Task ── belongs to ── Category
Task ── uses ─────── NotificationStrategy
Task ── binds ────── GeofenceLocation
GeofenceLocation ── has ── usage/statistics history
User ── unlocks ─── Achievement
```

位置、任务和提醒的关联需要通过显式关联模型维护，不能仅靠页面局部状态。删除地点、分类或策略前应评估其被引用的任务，并在产品层给出提示。

## 6. UI 与交互维护规则

- 默认视觉：蓝白主题，主色 `#0A84FF`，浅色背景、白色表面、细边框；不将紫色作为普通页面主色。
- 一级页面保留底部导航；二级页面使用安全区域与统一返回栏。
- 次级页面使用 `Scaffold + TopAppBar`，标准返回箭头和统一标题基线，禁止遮挡状态栏。
- 不使用 Emoji 或字母方块作为功能图标；使用现有 Material 图标或 Android Vector Drawable。
- 页面改造按“审核 → 文件清单 → 用户批准 → 实现 → 编译”流程进行。
- 首页、创建页、底部导航属于高影响区域；没有明确需求不得顺带改动。

## 7. 验证与发布

### 编译

```powershell
.\gradlew.bat :app:compileDebugKotlin --console=plain
.\gradlew.bat :app:assembleDebug --console=plain
```

### 自动化测试

项目以 JVM 单元测试作为日常回归门禁，覆盖任务状态、重复任务、筛选、统计计算、AI 路由、同步、地理围栏检查、位置服务降级、Repository 和 Worker 失败策略。提交前至少执行：

```powershell
.\gradlew.bat :app:testDebugUnitTest --console=plain
.\gradlew.bat :app:lintDebug --console=plain
.\gradlew.bat :app:assembleRelease --console=plain
```

GitHub Actions 会在 `main`、`develop` 的推送和面向 `main` 的 Pull Request 上自动运行上述质量检查，并上传单测与 Lint 报告。新增业务规则时，优先在 `src/test` 为纯 Kotlin、UseCase、ViewModel 状态与服务降级路径补测试；涉及 Room、导航和 UI 的少量关键路径放在 `src/androidTest`。

以下能力依赖真实设备/发布包，不能仅靠 JVM 单元测试判定可用：系统权限弹窗、通知与后台提醒、高德地图 Native 渲染及定位、地理围栏触发、SenseVoice/ASR Native 初始化。本项目发布前必须按下方真机回归清单验证这些路径。

### 最小真机回归

1. 任务创建、编辑、完成、延期、取消、重复规则。
2. 位置授权、当前定位、地图选点、新建地点和任务绑定。
3. 通知授权、提醒策略、早晚报与后台提醒。
4. 任务周/月与统计今日/本周/本月的一致性。
5. 同步、冲突、恢复确认和三种格式导出。
6. 小屏、长文本、深色模式、无网、无权限和空数据状态。

### 发布约束

- 不提交 APK、签名文件、私钥、真实 Key、模型、native runtime、日志或本地构建产物。
- APK 通过 GitHub Releases 分发，源码分支只保存可审阅代码和上述四份根目录文档。
- 每次影响用户行为的修复或功能变更，更新 `更新日志.md`。
