# NextThingB1 更新日志

本文档记录应用的版本更新历史和功能改进。

**发布节奏**：每一月发布一个新版本

---

## 版本 0.4.0 (2026-05-17)

### ✨ 新增功能

**云同步（客户端 + 后端）**
- 完整的前后端同步系统：增量同步、冲突检测、全量同步、冲突解决
- 客户端所有 CRUD 操作写入后自动标记 `syncStatus=PENDING`，等待同步上传
- 后端新增 9 个 REST 端点（`/api/sync/**`），Spring Boot + MySQL + JWT 鉴权
- 新增 `SyncedTask` / `SyncedCategory` JPA 实体，字段对齐客户端 DTO
- 新增 `SyncService` 核心同步逻辑：增量 upsert、时间戳比较、冲突生成、软删除同步
- `lastSyncTimestamp` 持久化至 DataStore（`SyncPreferences`），进程重启后不丢失
- 新增 `SyncScreen` 同步状态页面，从设置页「数据同步」进入
- 支持：同步状态指示器、上次同步时间、手动同步按钮、冲突列表 + 解决按钮

**今日任务桌面 Widget**
- 基于 Compose Glance 1.0 的桌面小组件，显示今日待办前 5 条
- 30 分钟自动刷新 + App 内任务变化时主动调 `updateAll()` 即时更新
- 点击 Widget 跳转 MainActivity
- `provideGlance` 直接查询 Room 数据库，绕开 GlanceState/Preferences 机制
- WorkManager 后台定期更新 Widget 数据

**日历月视图**
- 手写月历网格（6×7 布局），未引入第三方日历库
- 月份导航（◀ 2026年5月 ▶）+ 星期头 + 日期格子 + 彩色小点标记有任务的日期
- 选中日期下方展示当日任务列表，点击任务跳转详情
- `TaskDao` 新增 3 个查询：`getTasksByDueDateRange`、`getTasksByDueDate`、`getDatesWithTasksInMonth`
- 入口在 TodayScreen 头部日历图标，不占用底部导航 Tab

**预置默认通知策略**
- 首次启动自动插入 3 条预置策略：无声提醒 / 标准提醒 / 重要提醒
- ID 用 `preset_` 前缀区分用户自建数据，`OnConflictStrategy.IGNORE` 防重复
- 降低新用户使用门槛

### 🔧 重构优化

**数据库迁移 v10→v11**
- `TaskEntity` / `CategoryEntity` 新增 `deleted: Boolean = false` 软删字段
- 所有 `TaskDao` / `CategoryDao` 的 SELECT 查询加 `AND deleted = 0` 过滤
- 软删除保留删除记录，支持同步删除操作到服务端

**通知策略 UI 优化**
- 创建任务页：通知策略和地理围栏从各占一整行改为并排 `Row(weight(1f))`，两卡片等高 80dp
- 新建策略页：震动 4 选项、声音 3 选项、通知方式 3 选项从列表式改为 chip 横排（`CompactSelectionSection`），节省 60%+ 空间

**SyncUseCases 去 Stub**
- `SyncUseCasesStub.kt` 中 5 个 use case 从硬编码返回值改为直接委托 `SyncRepository`

**Gradle 配置优化**
- 代理配置 + `settings.gradle.kts` 仓库顺序调整（官方源优先，镜像放后面）

### 🐛 问题修复

**定位系统无法获取位置**
- 权限检查逻辑修复：`COARSE || FINE`（OR 逻辑），兼容 Android 12+ 分级授权
- 高德 Key 认证三要素（SHA1 + 包名 + API Key）匹配修复
- Google FusedLocationProvider 回退超时优化

**退出登录按钮无效**
- 登出时清除 TokenManager 中的 access_token / refresh_token
- 清空导航栈 `popUpTo(0) { inclusive = true }`，防止返回键回到已登录页面

**登录失败只显示"网络错误"**
- 解析 HTTP 400 响应体，正确显示服务端错误信息（如"用户名已存在"）
- 区分网络错误和业务错误

**设置页与用户信息页头像不一致**
- emoji 与 drawable 头像混用问题修复

**Hilt 编译错误（kapt → KSP 迁移遗留）**
- BroadcastReceiver 用 `@EntryPoint` + `EntryPointAccessors` 替代 `@AndroidEntryPoint`
- 补全缺失的 `@Inject constructor` 和 `@ApplicationContext` 限定符

**通知策略弹窗自适应高度**
- 去掉固定 60% 屏幕高度，改为 `wrapContentHeight` + `heightIn(max=500.dp)`

**试听按钮静音模式崩溃**
- 勿扰模式下调 `setStreamVolume()` 抛 `SecurityException`，删除该调用
- 静音/震动模式下播放铃声无反馈，新增检测 + Toast 提示

**Glance Widget 编译失败（3 轮修复）**
- `GlanceId` unresolved：Aliyun 镜像缓存不完整，从 1.1.0 降级到 1.0.0
- `clickable` / `actionStartActivity` unresolved：Kotlin 通配符导入不递归覆盖子包，补显式导入
- `Preferences` / `currentState` unresolved：放弃 GlanceState 方案，改为 `provideGlance` 直查 Room

**启动 ANR（runBlocking + 无限 Flow）**
- `SyncRepositoryImpl.init` 用 `runBlocking` + `collect` 读 DataStore 无限 Flow，主线程永久阻塞
- 删除 init 块，改用 `.first()` 懒加载 `lastSyncTimestamp`

---

## 版本 0.3.0 (2026-03-22)

### ✨ 新增功能

**AI 智能总结**
- 统计页概览 Tab 新增 AI 深度分析功能，与规则洞察合并为统一的「任务洞察」卡片
- 点击「生成 AI 分析」按钮，将当前任务统计数据发送给 LLM，返回 100-200 字的自然语言总结
- AI 分析涵盖：整体情况概述、亮点、问题指出、1-2 条可执行建议
- 切换时间维度后自动清除 AI 总结，规则洞察立即更新
- 支持加载中、错误重试、刷新等完整状态管理
- 新增 `AIStatsAnalyzer` 接口 + `AIStatsAnalyzerService` 实现，复用项目已有的 AI 基础设施

**分类编辑独立页面**
- 分类编辑从弹窗升级为独立全屏页面，三段式 Card 布局：基本信息、选择图标、选择颜色
- 新增颜色选择器：18 色预设色板（3×6 圆形网格）+ 自定义 Hex 输入框，实时预览
- 分类列表每项左侧新增彩色竖条，颜色跟随分类 `colorHex`
- 新增 `CategoryEditScreen` + `CategoryEditViewModel`，支持新建/编辑两种模式
- 路由 `category_edit/{categoryId}` 集成到导航系统

**趋势 Tab 重构**
- 新增「完成率走势」图表：周完成率面积图 + 均值水平虚线 + 周环比变化
- 新增「平均完成周期」图表：Cycle Time 柱状图 + 趋势虚线，管理学周期时间度量
- 新增「累积流图」图表：Kanban CFD 三层堆叠面积图（已完成/逾期/待办）+ 智能解读提示
- 原有任务完成趋势折线图增强：叠加 7 日移动平均虚线，平滑短期波动
- 时间维度选择器从第一个图表内部抽出，独立为公共选择器，切换后 4 个图表同步更新
- 每个图表右上角新增统一的趋势标签（↗上升/↘下降/→平稳）+ info 按钮
- 新增趋势详情页（`TrendDetailScreen`）：点击 info 按钮跳转，展示图表含义、判断规则、当前结论、数据支撑

**效率 Tab 增强**
- 综合效率评分雷达图新增蛛网结构：3 层同心六边形（33%/66%/100%）+ 6 条对角线
- 新增「效率周对比卡」：2×2 网格展示日均完成、准时率、响应速度、目标达成 4 个指标的变化
- 新增「效率建议卡」：基于雷达图最弱维度 + 黄金时段数据自动生成 2-3 条可执行建议
- 效率分计算从 2 维升级为 5 维：完成率(35%) + 准时率(25%) + 执行率(15%) + 响应速度(15%) + 积压控制(10%)
- 效率分说明弹窗重写：5 个维度逐行展示（图标 + 名称 + 说明 + 权重进度条 + 百分比）

**分类 Tab 优化**
- 饼图颜色从硬编码名称映射改为基于分类 `colorHex` 动态生成柔和色
- 饼图图例重设计：分类图标 + 名称 + 内联进度条 + 完成率 + 任务数
- 分类效率排行榜重写：扁平化 Item（左侧分类颜色竖条 + 排名徽章 + 图标 + 进度条 + 效率分）
- 排行榜默认展示前 3 名，超过 3 个时提供「查看全部」展开按钮
- 分类热力图改为 GitHub 风格绿色色阶（5 档：浅绿→深绿），底部新增色阶图例
- 所有分类图标从硬编码 emoji 替换为 `CategoryIconView`，与创建任务页一致

### 🔧 重构优化

**概览页面精简**
- 移除概览 Tab 的「任务健康度仪表盘」和「重要程度四象限分布」两个组件
- 移除概览 Tab 的「核心指标卡片」（待办/重要紧急/逾期/完成率 4 个数据卡）
- 智能洞察卡片与 AI 总结合并为统一的「任务洞察」卡片，时间选择器始终显示

**趋势 Tab 清理**
- 移除「积压预警趋势面积图」（与累积流图信息重叠）
- 移除「完成速度变化柱状图」（与完成趋势折线图重复）
- 移除「月历热力图」（与分类 Tab 的热力图重复）

**效率 Tab 清理**
- 移除「任务完成漏斗」（与趋势 Tab 累积流图重叠）
- 移除「延迟成本分析」（信息量小，价值低）

**粒子动画调优**
- 首页任务完成粒子动画时长从 1400ms 缩短至 500ms
- 粒子速度、重力、摩擦力参数调优，500ms 内完全爆炸展开

**首页顶部栏调整**
- 移除搜索图标，位置信息移至右侧
- 长地址名称新增 `TextOverflow.Ellipsis` 截断，最大宽度 140dp

### 🐛 问题修复

**首页数据不刷新**
- 修复新建任务后首页偶发不显示新数据的问题（Room InvalidationTracker 丢失事件）
- 使用 `refreshTrigger + flatMapLatest` 强制重订阅，每次 `onScreenResumed` 触发刷新

---

## 版本 0.3.1 (2026-03-23)

### ✨ 新增功能

**主题设置独立页面**
- 将「主题设置」从设置页弹窗抽离为独立的 `ThemeSettingsScreen` 全屏页面
- 新增 `ThemeSettingsViewModel`，管理主题模式选择、天气配色预览、自定义颜色编辑状态
- 主题模式选择卡片新增分割线，4 个选项（跟随系统 / 浅色 / 深色 / 跟随天气）视觉层次更清晰
- 跟随天气模式下展示 8 张天气配色卡片（2 列网格），点击任意卡片触发效果预览，面板自动滚动至底部
- 每张天气配色卡片支持点击🎨按钮自定义主色，底部弹出 24 色预设调色板
- 调色板选色后实时反映在预览面板，支持「恢复默认」一键还原
- 自定义主色通过 `ThemePreferences` 持久化存储（DataStore，每种天气独立 key）
- 效果预览面板模拟 AppBar + Card + 按钮组，直观呈现当前配色方案
- 路由 `theme_settings` 集成到导航系统，底部导航栏在该页面自动隐藏
- 顶部栏样式与 `AchievementScreen` 保持统一（`Surface 56dp + BgCard + shadowElevation=1dp`）

**跟随天气主题**
- 新增 `ThemeMode.WEATHER` 枚举值，支持「跟随天气」主题模式
- 预置 9 套天气配色方案（晴/阴/多云/雨/雷雨/雪/雾/风/未知），整体色调柔和浅淡
- 天气状态由 `TodayViewModel` 在成功获取天气 API 后写入 `ThemePreferences`
- `MainActivity` 订阅 `currentWeatherCondition` + `weatherCustomPrimaries`，实时应用对应配色
- 新增 `weatherAppColorsWithCustom()` 工具函数，在基础配色之上叠加用户自定义主色

**首页天气背景图**
- 「今日完成」进度卡片背景由纯色改为天气图片（8 类 + 1 兜底），与天气 API 返回类型联动
- 图片上叠加垂直渐变遮罩（20%→55% 黑色透明），确保白色文字可读性
- 天气图片资源（`weather_*.jpg`）统一存放于 `res/drawable/`

**「我的」页面优化**
- 新增预置头像 `preset_avatar.jpg`，首次使用或未设置头像时自动展示
- 头像形状由圆形改为圆角矩形（`14dp`，类微信风格），尺寸 64dp
- 支持从系统相册选择头像（`PickVisualMedia`），选择后持久化至数据库
- 昵称与成就徽章同行对齐（`SpaceBetween Row`），移除冗余的「连续 X 天」文字标签

**首页农历信息**
- 「今日完成」进度卡片左侧文字区域替换为农历日期展示
- 第一行显示干支纪年 + 生肖 + 农历月日，如「乙巳蛇年二月初八」
- 第二行条件显示：当天为二十四节气时展示节气名（优先），否则展示节日名（元宵、端午、七夕等）
- 节气与节日不同时显示（清明、冬至等节气日只显示节气名）
- 新增 `ChineseDateHelper` 工具类，基于 Android 内置 ICU 库（`android.icu.util.ChineseCalendar`，API 24+），零外部依赖
- 节气数据：2024–2028 精确日期硬编码查表，共 120 条记录
- 节日支持三类：公历固定节日（元旦/劳动节/国庆等）、农历固定节日（春节/中秋/重阳等）、浮动节日（母亲节/父亲节/感恩节）
- 除夕通过判断次日是否为正月初一动态确定，无需硬编码

**任务重复频次**
- 创建任务与任务详情页新增「重复频次」配置项，图标 🔁
- 快捷选项 5 个：单次 / 每日 / 工作日 / 周末 / 法定节假日
- 新增「自定义 →」选项，点击跳转独立全屏配置页 `RepeatCustomScreen`
- 自定义页支持「按周」模式（7 个星期按钮多选）和「按月」模式（31 天数字网格多选）
- 底部实时预览文字，如「每周一、三、五重复」，未选择时禁用保存按钮
- 自定义结果通过 `SavedStateHandle` 回传，返回后自动同步到创建/详情页
- 新增 `RepeatFrequencyType.WEEKDAYS / WEEKENDS / LEGAL_HOLIDAY` 三个枚举值，`RepeatCustomScreen` 对应 `WEEKLY / MONTHLY`
- `LegalHolidayHelper`：内置 2025–2027 年法定节假日精确日期，`isLegalHoliday(date)` 供调度器调用
- 路由 `repeat_custom?type=&weekdays=&monthDays=` 集成到导航系统，底部导航栏自动隐藏

### 🐛 问题修复

**默认头像不显示**
- 修复 `AsyncImage` 加载失败时静默空白的问题：统一设置 `fallback` / `error` / `placeholder` 三参数均指向 `preset_avatar`
- 修复数据库中空字符串 `avatarUri` 被 `Uri.parse("")` 解析为非 null 导致走错分支的问题（添加 `takeIf { isNotBlank() }` 守卫）

**主题设置页顶部留白**
- 移除 `TopAppBar` 内置 `windowInsets`，改用自定义 Row header，消除双重状态栏 inset 叠加导致的多余留白

## 版本 0.3.2 (2026-03-24)

### 🐛 问题修复

**重复任务修改分类次日不生效**
- 根因：每日重复任务存在两条记录——**模板**（`isTemplate=true`）和**今日实例**（`isTemplate=false`）。详情页编辑只更新了实例，模板保持原样；`GenerateRecurringTasksWorker` 次日从未变更的模板 `.copy()` 生成新实例，分类因此回退
- 修复：`TaskDetailViewModel.saveChanges()` 在实例保存成功后，若 `templateTaskId != null` 则加载模板，将标题、描述、分类、重复频次、重要性、图片、标签、通知策略等结构性字段同步写回模板；`status`、`dueDate`、`instanceDate` 等实例专属字段不同步

### 🔧 重构优化

**编译警告清理**
- `TaskUseCases.kt`：移除空值检查后多余的 `!!` 非空断言
- `CreateTaskScreen.kt`：删除未使用变量 `isGeofenceExpanded`、`density`；对 10 个含未使用参数的内部 Composable 函数添加 `@Suppress("UNUSED_PARAMETER")`
- `TaskDetailScreen.kt`：删除未使用变量 `savedLocations`；对 4 个函数添加 `@Suppress("UNUSED_PARAMETER")`
- `TodayScreen.kt`：弃用的 `LocalLifecycleOwner` 添加 `@Suppress("DEPRECATION")`；对 2 个函数添加 `@Suppress("UNUSED_PARAMETER")`
- `CreateTaskViewModel.kt` / `NotificationHelper.kt`：`when` 表达式补全 `WEEKDAYS`、`WEEKENDS`、`LEGAL_HOLIDAY` 缺失分支，修复编译错误

---

## 版本 0.2.0 (2026-03-17)

### ✨ 新增功能

**分类管理独立页面**
- 将分类管理从创建任务页面中抽离为独立的「管理分类」页面
- 支持分类的完整 CRUD：新建、编辑、删除、排序
- 新建/编辑分类时可选择 12 个预置图标（生活、工作、学习、运动、娱乐、财务、社交、出行、家庭、杂项、字节、股票）
- 支持从相册选择自定义图标作为分类图标
- 预置分类受保护，不可删除，仅支持重命名和更换图标
- 分类支持上下拖动排序，排序结果同步影响创建任务页面的分类展开顺序

**任务页面全面重设计**
- 移除占据 30% 屏幕的 WeeklyOverviewCard 大卡片，替换为紧凑的标题栏 + 筛选栏布局
- 新增搜索功能：点击搜索图标展开搜索框，支持按任务标题和描述实时搜索（300ms 防抖）
- 新增状态筛选：全部 / 待办 / 已完成 / 逾期 / 已取消
- 新增分类筛选：下拉选择分类，带分类图标展示
- 新增优先级筛选：按四象限（重要且紧急 / 重要但不紧急 / 不重要但紧急 / 不重要且不紧急）筛选
- 多个筛选器支持叠加，顶部显示激活筛选器数量并可一键清除
- 逾期任务在列表顶部独立展示为可折叠的红色区块
- 月视图新增月份导航栏（◀ yyyy年MM月 ▶），操作更直观
- 任务为空时显示友好的空状态提示

**每日名句**
- 任务页面展示每日一句毛泽东选集经典名句
- 收录约 100 条经典语录（≤20字），每天不重样，年循环轮换

**通知策略弹窗优化**
- 弹窗高度由内容实际高度决定，最大不超过屏幕 60%
- 「新建策略」按钮固定在弹窗底部，不再被策略列表滚动遮挡
- 策略列表项之间新增分割线，视觉层次更清晰
- 策略较多时列表区域支持滚动

### 🔧 重构优化

**分类图标渲染系统**
- 新建共享组件 `CategoryIconView`，统一处理三种图标类型：drawable 预置图标、相册 URI 图标、emoji 文字图标
- 首页任务卡片、统计页面、成就页面、任务详情页面全部统一使用 `CategoryIconView` 渲染分类图标
- 移除首页旧的 `getCategoryEmoji()` 硬编码图标映射函数

**分类数据迁移**
- 新增 `migrateOldIcons()` 自动迁移机制，将数据库中的旧图标格式（如 `laptop-code`、`dumbbell`、`#9E9E9E`）自动转换为新的 `drawable:xxx` 格式
- 支持按分类名称智能推断图标（如「字节」→ `drawable:bytedance`，「股票」→ `drawable:stock`，「健身」→ `drawable:sports`）

**创建任务页面瘦身**
- 分类选择器简化为仅展示图标 + 名称，移除旧的删除/置顶按钮
- 「新建分类」按钮改为「管理分类 →」，跳转到独立管理页面
- 删除内嵌的 `CreateCategoryDialog`、`CategoryMenuItem` 等旧组件（~100 行）
- 移除 AI 分析创建文字，输入框提示改为"输入任务内容"，高度减半

**任务详情页面同步更新**
- 任务详情页的分类选择器同步简化，增加「管理分类」跳转入口
- 移除详情页内嵌的分类创建弹窗及相关 ViewModel 方法

**任务页面代码清理**
- 移除 TasksViewModel 和 TasksScreen 中约 100+ 行调试 Log 语句
- 删除死代码：`UnifiedTopSection`、`ViewSwitchSection`、`TaskProgressBar` 等未使用组件
- 分类排序逻辑从使用频率排序改为遵循管理页面的 `sortOrder`

### 🐛 问题修复

**分类图标无法渲染**
- 修复首页任务卡片仍使用旧的硬编码 emoji 映射导致图标显示为文字的问题
- 修复统计页面和成就页面的分类图标仍按旧格式渲染的问题

**旧分类数据显示异常**
- 修复数据库中存储的旧图标值（如 `dumbbell`、`laptop-code`、`#9E9E9E`）直接显示为文本而非图标的问题
- 通过自动迁移机制将所有历史数据转换为可渲染的新格式

**管理分类页面顶部空白过多**
- 修复 TopAppBar 默认 windowInsets 导致的额外顶部留白，设置 `windowInsets = WindowInsets(0.dp)`

**分类排序不一致**
- 修复创建任务页面使用「使用频率排序」覆盖管理页面手动排序的问题
- 统一使用 Room 数据库中的 `sortOrder` 字段

---

## 版本 0.1.0 (2025-11-26)

### ✨ 新增功能

**任务放弃原因记录**
- 左滑任务点击"放弃"按钮时，现在会弹出对话框让你输入放弃原因
- 放弃原因会自动保存到任务描述中，方便后续回顾
- 支持选填，可以直接确认放弃

**任务延期原因记录**
- 左滑任务点击"延期"按钮时，现在会弹出对话框让你输入延期原因
- 延期原因会自动保存到任务描述中
- 任务截止日期自动推迟一天
- 采用橙色主题，与放弃任务的红色主题区分

### 🐛 问题修复

**修复任务逾期状态不刷新问题**
- 修复了应用从后台恢复时，已逾期任务仍显示为待办状态的问题
- 现在切回首页时会自动刷新任务状态，实时显示逾期任务
- 无需清除应用后台即可看到正确的任务状态

**修复任务修改截止时间后状态不更新**
- 修复了逾期任务修改截止时间到未来后，仍显示"已逾期"的问题
- 现在修改截止时间后，任务状态会自动重新计算

---

## 更新说明

- 每一月发布一个新版本
- 建议及时更新以获得最佳体验和最新功能
- 如有问题或建议，欢迎反馈
