# NextThing 当前位置弹窗断点（2026-08-14）

## 本次完成

- 重构首页“当前位置”详情弹窗，移除标题行重复的“刷新”与底部“关闭”文字操作。
- 定位地址作为主信息，展示高德实际返回的完整 `address`；最多显示三行，超出省略。
- 新增地址复制图标：复制完整原文到系统剪贴板，并在弹窗内提示“地址已复制”。
- 经纬度保留为并列的“经度 / 纬度”两项，完整保留六位小数，但降为地址下方的次级信息。
- 精度与相对更新时间合并为一行；底部唯一主操作为“刷新定位”，加载时显示“正在定位…”。
- 引入官方 Compose `material-icons-extended`，仅用于标准复制图标。

## 已验证

- `:app:compileDebugKotlin --no-daemon --console=plain -Pksp.incremental=false` 编译通过。

## 待真机回归

1. 地址超过两行时，视觉应稳定且复制内容仍为完整地址。
2. 点击复制图标后，粘贴验证地址原文，并确认“地址已复制”提示可见。
3. 点击“刷新定位”时，按钮应进入加载态并只保留一个刷新入口。
